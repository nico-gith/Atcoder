#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
  int A, B;
  cin >> A >> B;

  // ここにプログラムを追記
  cout << "A:";
  rep(i, A)
  cout << "]";

  cout << endl << "B:";
  rep(i, B)
  cout << "]";

  cout << endl;
}

